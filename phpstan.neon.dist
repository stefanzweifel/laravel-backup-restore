includes:
    - ./phpstan-baseline.neon

parameters:
    # This package ships its own config file, which is not the "config"
    # directory larastan assumes when it looks for env() calls.
    configDirectories:
        - config

    level: 9
    paths:
        - src
        - config
        - database
    tmpDir: build/phpstan
